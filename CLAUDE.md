# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# Claude Code 작업 지시서 (KR) — KBeacon 기반 배회방지 앱 (sensordemo 개조)

이 문서는 **Claude Code**가 본 저장소에서 코드를 수정·생성할 때 따를 **단일 기준(Single Source of Truth)** 입니다.  
사용자는 데모프로젝트를 *완전히 새로* 시작하여 **sensordemo** 모듈만을 기반으로 커스텀 앱을 만들고자 합니다.  
아래의 요구사항은 사용자가 제공한 `NEW_md.txt` 원문을 **누락 없이** 반영하되, 개발에 바로 적용되도록 구조화·명료화했습니다.

---

## 0) 핵심 원칙
- 모든 설명·요청은 **한국어** 기준. (BLE, RSSI 등 기술용어는 영어 표기 허용)
- **sensordemo** 중심으로 개발합니다. iBeacon/eddystone 데모는 **사용하지 않습니다**.  
- README의 SDK 사용법/예제 코드는 반드시 참고합니다.
- 본 문서의 *모든 항목은 구현 범위*입니다. 구현 세부가 모호한 경우, 보수적으로 해석하여 최소기능을 먼저 완성합니다.

> 근거: NEW_md.txt의 A절(들어가기 전에) 및 데모 선택 지침을 반영.

---

## 1) 프로젝트/의존성/권한 체크리스트
- **의존성**: `com.kkmcn.kbeaconlib2:kbeaconlib2:1.3.3` (Maven Central)
- **Android 권한(안드12 포함)**: `BLUETOOTH`, `BLUETOOTH_ADMIN`, `ACCESS_COARSE_LOCATION`, `ACCESS_FINE_LOCATION`, `BLUETOOTH_SCAN`, `BLUETOOTH_CONNECT`
- **IDE/SDK**: Android Studio, minSdk 24 이상, (프로젝트 내 기존 설정 준수)

> 참고: README.md의 도입/권한/SDK 설정 절(필수 권한, minSdk, Maven Central 등).

---

## 2) 아키텍처 권장 (Foreground Service 기반)
> 안정적 백그라운드 동작 요구를 만족하기 위한 설계 제안

- **BleService (Foreground Service)**  
  - 스캔/연결/명령 송신/배터리 조회/스케줄링(부저·태블릿 알람 재트리거) 단일화
  - **Repository**: `MAC → { name, lastScanRssi, rssiFiltered, distanceFiltered, batt%, txPowerAt1m, n, R², RMSE, threshold }`
  - **Persistence**: Room 또는 SharedPreferences(+JSON)로 최소 20개 장치 저장
- **UI(Activity/Fragment)**  
  - Service 바인드 → LiveData/Callback으로 상태 구독, UI 전담
  - 대폰 알람(=태블릿 알람) 버튼/중지 버튼, 각 비콘별 부저 버튼/중지 버튼, 거리 설정, 이름변경 다이얼로그
- **스캐닝**  
  - 앱 시작 시 자동 스캔 (연속)
  - 6자리 숫자로 시작하는 **이름 필터** 적용 (`^\d{6}_.*`)
- **연결/명령**  
  - 부저/배터리 조회 시에만 필요한 짧은 연결, 완료 즉시 **disconnect**
  - 동시에 여러 기기 *연결 시도 가능*하되, GATT 동시 연결 수는 단말 한계를 고려하여 **큐잉** 지원
- **백그라운드 보존**  
  - 포그라운드 알림(서비스 채널) 필수, 앱 재실행/최근앱 복귀 시 *상태 유지*

---

## 3) 기능 명세 (요약 정리)

### 3.1 스캔/목록
- 시작 즉시 스캔(연속), 수동 시작/중지 버튼 제공
- 목록 항목: **장치이름**, **전처리된 RSSI(rssiFiltered, 0.1 dBm)**, **배터리%**, **거리(distanceFiltered, 0.1 m)**
- **업데이트 주기 500ms**
- **이름 필터**: 6자리 숫자로 시작하는 장치만 표시 (예: `526536_홍길동`)
- 배터리%: sensordemo 로직을 그대로 활용(**전압→% 계산**),  
  - 기본적으로 *부저 명령 송신을 위한 연결 시* 취득  
  - 최초 탐지 시 1회 연결 후 취득·저장·즉시 disconnect  
  - 이후 **1시간 주기**로 갱신
- 저장/복원: 앱 종료/재실행/완전 종료 후에도 동일 이름 장치에 대해 **배터리/거리설정/캘리브레이션** 값 복원

### 3.2 장치 이름 변경
- 목록에서 항목 클릭 → 다이얼로그
- **최대 18자, 한글 입력 지원**, 예: `566566_홍길동`

### 3.3 거리 설정값
- 항목 우측에 거리 설정값 표시/클릭 → 다이얼로그
- 범위 **0.0~99.9 m**, **0.1 m 단위**, 기본값 **50.0 m**
- 앱 내부 저장/복원

### 3.4 비콘 **부저**(beep) 제어
- 항목 우측: **알람**/ **알람 중지** 버튼
- 알람: 연결 → `ringDevice` 명령 송신(`ringType=0x1`) → **연결 종료**
- **지속 울림**: `ringTime`은 유한(ms) 이므로 **스케줄러로 재트리거**(알람 중지 시까지 반복)
- 상태표시: `"알람"`→`"동작중"`→`"연결됨"`→`"알람중"`→(중지 시)`"알람"`
- **여러 비콘을 동시에** 울릴 수 있음 (비콘별 독립)

### 3.5 **태블릿 알람**(기기 스피커) 제어
- 목록 하단: **폰 알람**/ **폰 알람중지** 버튼 (표기는 한국어 그대로 사용)
- 기본 20초, **스케줄러로 반복**(중지 시까지)
- 상태표시 일관

### 3.6 RSSI→거리 계산 + 필터링
- 광고 RSSI만 사용(연결 중에는 사용 안 함 — SDK 기준 가정)
- **전처리(RSSI)**: 최근 N=10 창, **중앙값±7 dB 이상치 제거**, **EMA α=0.25** → `rssiFiltered`
- **거리**: `distance = 10^((txPowerAt1m - rssiFiltered)/(10*n))`, **EMA α=0.30** → `distanceFiltered`
- **광고주기**: 200 ms (기기 스펙 가정)

### 3.7 캘리브레이션(거리 보정)
- 버튼 라벨: **거리보정**
- 절차: 1m/2m/3m에서 각 **10초 측정**, 구간별 중앙값(r1,r2,r3) 취득 → **선형회귀**로 `txPowerAt1m=a`, `n=-b/10`
- **품질평가**: R², 이상치(z-score 규칙, |residual| 규칙), RMSE
- **판정**: GOOD / BORDERLINE / BAD  
  - GOOD/BORDERLINE: 저장(txPowerAt1m, n, R², RMSE) 후 안내, BORDERLINE은 재측정 권유  
  - BAD: 저장 없이 재측정 안내

### 3.8 자동 기능
- `distanceFiltered > 사용자 설정 거리` 시:  
  **비콘 부저 알람 송신**(연결→명령→해제) **+ 태블릿 알람 실행**
- 캘리브레이션 중에는 자동 알람 **비활성**
- 자동 기능 중에도 모든 수동 기능은 **항상 동작 가능**

> 위 3.x 항목은 NEW_md.txt의 C, D, E, F, G, H 절 전체를 개발 친화적으로 재구성한 것입니다. 원문은 본 문서 맨 아래 **부록 A**에 1:1로 보존했습니다.

---

## 4) SDK/예제 연동 포인트 (구현 시 바로 쓰는 레퍼런스)
- **Ring 명령 파라미터**: `msg="ring"`, `ringTime(ms)`, `ringType(0x1=beep)`, optional `ledOn/ledOff`  
  - 예제 메서드: `ringDevice()` — 연결 필요, 명령 송신 후 결과 콜백 처리
- **장치 이름 길이 제한**: `<= 18 chars`
- **권한·스캔·연결·설정**: README의 예제 코드 흐름을 따라 구현

---

## 5) 구현상의 주의/보완(NEW_md.txt 개선 제안 포함)
1) **백그라운드 제한**: Android 8+에서는 백그라운드 스캔/작업에 제약이 큼 → **Foreground Service + Notification** 필수.  
2) **RSSI(연결 중) 취득**: NEW_md.txt는 ‘연결 중 RSSI 불가’로 가정. Android GATT API에는 `readRemoteRssi`가 있으나 SDK 경로/기기 특성에 따라 불안정할 수 있음. 본 앱은 **광고 RSSI 기준**으로 고정(명세 유지).  
3) **500ms UI 갱신 주기**: 가능하나 태블릿 성능·배터리를 고려해 **1s 옵션**도 제공 권장(설정값) — 기본은 요구사항대로 500ms.  
4) **“폰 알람” 명칭**: 실제 타깃 디바이스가 **태블릿**이므로, UI 표시는 NEW_md.txt 용어를 유지하되 코드/주석은 *태블릿 알람*으로 통일(혼동 방지).  
5) **동시 다중 연결**: 단말 BLE 스택 한계 고려 필요. 서비스에서 연결 **큐**(Semaphore)로 실패시 재시도·백오프 구현 권장.  
6) **캘리브레이션 저장 키**: 장치 **이름** 기준 저장 요구 → 이름 변경 시 과거 보정치 매칭 불가 가능성. 실제로는 **MAC+이름** 혹은 **MAC 우선** 권장. (단, 명세 우선 정책으로 기본은 *이름 키* 사용)  
7) **배터리 % 환산**: README의 센서/시스템 패킷에서 배터리 지표 제공. 전압→% 환산 로직은 **sensordemo 함수 재사용**으로 충족.  

---

## 6) 개발 순서 제안 (체크리스트)
1. Foreground Service, Notification 채널, 바인딩 골격 구성  
2. 스캔 파이프라인(필터, 목록, 500ms 틱 렌더)  
3. 이름변경 다이얼로그(≤18자)  
4. 거리 설정값(저장/복원)  
5. **ringDevice** 연동(스케줄러 반복), 다중 비콘 동시 처리  
6. 태블릿 알람(스케줄러 반복)  
7. 배터리 조회(최초 1회 + 1시간 주기)  
8. RSSI 전처리/거리 변환(EMA, 이상치 제거), 캘리브레이션(회귀, 품질판정/저장)  
9. 자동 기능(거리 초과 시 알람 시퀀스)  
10. 회귀 결과/설정/상태 영속화(Room/SharedPref)  
11. 예외/오류 핸들링, QA 시나리오, 성능 튜닝

---

## 7) 커밋 컨벤션 (Conventional Commits, 한국어)
- **제목**(≤50자) + **본문 선택**(줄당 ≤72자, 불릿 가능) + **Footer**(BREAKING CHANGE/이슈)  
- 예) `feat(scan): 6자리 시작 이름 필터 및 500ms 목록 갱신 추가`

---

## 8) 참고(README/CLAUDE 발췌 근거)
- 라이브러리 버전/권한/스캔·연결·설정 워크플로우
- ringDevice 파라미터와 예제 코드
- 장치 이름 길이 제한(≤18자)

---

## 9) 개발 환경 및 기술 정보 (Technical Development Guide)

### 9.1 프로젝트 구조
- **Main Project**: `KBeaconProDemo_Android/` - Android Gradle 프로젝트
- **Target Module**: `sensordemo/` - 이 모듈만 사용하고 개조
- **Unused Modules**: `ibeacondemo/`, `eddystonedemo/` - 사용하지 않음
- **Main Activity**: `DeviceScanActivity.java` - 단일 화면 구성
- **Service**: `BleService.java` - Foreground 서비스로 BLE 백그라운드 동작

### 9.2 빌드 및 개발 명령어
```bash
# Android 프로젝트 빌드 (Windows)
cd KBeaconProDemo_Android
gradlew sensordemo:build

# 디버그 빌드
gradlew sensordemo:assembleDebug

# APK 설치 (디바이스/에뮬레이터)
adb install sensordemo/build/outputs/apk/debug/sensordemo-debug.apk

# 로그 확인
adb logcat -s "KBeaconDemo"
```

### 9.3 핵심 의존성 및 라이브러리
- **KBeacon SDK**: `com.kkmcn.kbeaconlib2:kbeaconlib2:1.3.3`
- **Android SDK**: minSdk 26, targetSdk 34, compileSdk 34
- **Required Permissions**: Bluetooth, Location (manifest에 정의됨)
- **Foreground Service**: `android:foregroundServiceType="connectedDevice"`

### 9.4 주요 컴포넌트 아키텍처
- **BleService.java**: 핵심 백그라운드 서비스
  - BLE 스캔/연결/명령 송신 통합 관리
  - Foreground 서비스로 구현되어 백그라운드 동작 보장
  - 비콘 상태 Repository 패턴으로 데이터 관리
- **DeviceScanActivity.java**: 메인 UI 액티비티
  - 서비스 바인딩으로 BleService와 통신
  - 비콘 목록 표시 및 사용자 인터랙션 처리
- **데이터 저장**: 
  - `Prefs.java`: SharedPreferences 기반 설정 저장
  - `BeaconDataStore.java`: 비콘별 데이터 저장소
- **캘리브레이션**: 
  - `CalibrationSession.java`: RSSI-거리 변환 캘리브레이션
  - `CalibrationDialog.java`: 캘리브레이션 UI

### 9.5 개발 시 주의사항
- **권한 처리**: Android 12+ BLE 권한 (`BLUETOOTH_SCAN`, `BLUETOOTH_CONNECT`) 필수
- **Foreground Service**: 백그라운드 BLE 동작을 위해 필수
- **RSSI 필터링**: 6자리 숫자 시작 장치명 필터링 (`^\d{6}_.*`)
- **동시 연결**: BLE 동시 연결 수 제한 고려한 큐잉 구현
- **UI 접근성**: 저시력자를 위한 큰 폰트 및 버튼 크기

### 9.6 디버깅 및 테스트
- **로그 태그**: `"KBeaconDemo"`, `"BleService"` 등으로 필터링
- **ADB 명령어**: `adb logcat -s "KBeaconDemo"` 로그 모니터링
- **BLE 테스트**: 실제 KBeacon 디바이스 필요 (nRF528XX 칩셋)
- **거리 캘리브레이션**: 1m/2m/3m 거리에서 테스트

---

## 부록 A) 사용자 원문(NEW_md.txt, 변경 없이 보존)

```
A .들어가기전에
이 문서는 한국어를 기반으로 작성되었다.
앞으로 모든 요청과 출력은 한국어를 기반으로 한다. 단, BLE, RSSI 등 기술용어는 영어를 사용할 수 있다.
이 프로젝트에 사용된 데모프로젝트의 구조, 의존성 정보, 함수 적용 설명을 이해하기 위해 README.md 를 반드시 참조한다.
데모 프로젝트를 수정,변경하여 아래 사용환경 및 목표에 맞는 앱을 제작한다.
이 프로젝트는 Ksensor 프로토콜을 기반으로 한다. 따라서 데모 프로젝트의 ibeacon, eddystone 예제는 활용하지 않고 sensordemo 예제를 바탕으로 개발한다.
sensordemo 프로젝트의 두번째 페이지 beacon detail의 버튼 중에 ringdevice 버튼을 제외하고 모두 사용하지 않는 버튼이다.
따라서 두번째 페이지의 ringdevice 버튼을 제외한 모든 버튼과 관련된 함수 및 기능들도 사용하지 않는다.


B. 사용환경 및 목표

이 앱은 요양병원의 치매환자 배회방지 솔루션에 적용된다. 
간호사가 다른 작업중에 치매환자가 특정 지역을 벗어난것을 감지할수 있게 한다.
치매환자는 BLE 비콘을 부착하고 있고, 간호사는 데스크에 앱이 설치된 태블릿이 있다.
치매환자가 특정 지역을 벗어나면 BLE 비콘의 부저를 울리게 하고, 태블릿의 알람을 울리게 하여 사람이 인지하게 한다.
간호사는 먼저 수동으로 태블릿의 알람을 끄고, 환자를 찾은 뒤 수동으로 비콘의 부저 알람을 끄게 된다.
따라서 앱은 BLE 비콘의 RSSI 값을 취득하여 거리를 측정하고 측정된 거리가 사용자 거리설정값을 초과하면 비콘의 부저 알람와 폰 알람을 발생시키는 역할을 한다.

C. 기본 기능

다음과 같은 기능을 기본적으로 갖춘다. 이 기능들은 명료하게 기능별로 분류하여 구현한다. 필요시 더 작은 기능으로 쪼개서 모듈화 할수 있다.

1. BLE를 스캔하여 비콘 정보 취득 및 목록화하여 표시
2. 스캔된 BLE의 장치이름을 변경 가능
3. 거리 설정값을 입력 가능
4. 비콘 부저를 울리고 끄게 할수 있음
5. 폰 알람을 울리고 끄게 할수 있음
6. RSSI 값을 거리값으로 변경하여 표시

D. 기본 기능 상세설명

1. BLE를 스캔하여 비콘 정보 취득
- BLE 스캔 버튼으로 스캔을 시작하거나 중지할 수 있다.
- 앱이 시작되면 자동으로 스캔을 계속해서 시행한다. 연속 스캔 모드로 동작.
- 언제든지 수동으로 스캔을 실행 또는 중지 할수 있다.
- 스캔하여 탐지된 BLE를 목록화 하여 화면에 보여준다. 이 목록은 추후에 설명될 버튼 입력 및 거리 설정값과 서로 영역이 겹치지 않아야 한다.
- 비콘 정보는 장치 이름, RSSI 값, 배터리 수준 3가지만 취득한다
- 목록에는 장치이름, 전처리된 RSSI , 배터리 수준 그리고 추후에 설명될 RSSI 값을 이용하여 계산된 거리 값 4가지를 표시한다. 계산된 거리값은 0.1meter 단위로 표시한다
- 장치목록은 해당 목록에 표현할 정보를 모두 표현할수 있게 충분히 커야 한다.
- 스캔하여 BLE를 탐지할때는 장비이름에 대한 필터를 적용한다. 이 프로젝트에 사용되는 비콘은 항상 6자리 숫자로 시작한다.
따라서 6자리 숫자로 시작하는 이름을 가진 비콘만 탐지 결과로 보여준다.
예시) 비콘 이름 : 526536_홍길동
- BLE 이름으로 필터링되어 탐지된 결과를 탐지된 비콘이라고 말한다.
- 배터리 수준은 취득된 배터리 전압정보를 sensordemo 의 함수를 그대로 이용하여 % 로 환산하여 표시한다
- 배터리 수준은 앱 내부에 저장되어 사용자가 앱을 종료 했다가 재실행해도 배터리 수준 값을 가지고 있고 또한 표시되어야 한다
- 배터리 수준은 기본적으로 탐지된 비콘을 부저 알람을 위해 커넥트 할때 취득한다. 또한 앱실행후 탐지된 비콘(이름의 시작이 숫자 6자리 형식의 비콘)에 대해 최초 탐지시 1회 커넥트를 시도하여 배터리 수준을 확인하고 디스커넥트 한다.
- 부저 알람을 오랫동안 실행하지 않을 때 배터리 수준을 반영하기 위해 비콘 탐지 후 1시간에 한번 탐지된 비콘을 커넥트하여 배터리 수준을 업데이트하고 업데이트 되면 디스커넥트 한다.
- 부저 알람이 수동 또는 자동으로 실행되어 배터리 수준을 업데이트 한 경우 그 시점으로 부터 1시간 마다 한번씩 비콘을 커넥트 하여 배터리 수준을 업데이트 한다.
- 전처리된 RSSI는 추후에 설명될 rssiFiltered 값을 지칭한다. 전처리된 RSSI는 0.1 dBm 단위로 표시한다
- RSSI 값을 이용하여 계산된 거리 값은 추후에 설명될 distanceFiltered를 지칭한다. 거리값은 0.1m 단위로 표시한다.
- 목록에 비콘 정보 업데이트 주기는 500ms 로 한다.

2. 스캔된 비콘의 장치이름을 변경 가능
- 탐지된 비콘 목록에서 어떠한 장치를 클릭하면 이름을 변경할수 있는 다이얼로그가 뜬다
- 이름 변경 다이얼로그에서 이름 입력은 최대 18 캐릭터이고 한국어 입력을 지원한다
- 다음 예시가 입력가능해야 한다. 예시) 566566_홍길동
- 장치 목록에서 장치를 클릭하는 화면의 크기는 버튼과 서로 영역이 겹치지 않아야 한다. 이것은 장치이름 변경과 버튼 입력 및 거리설정값이 겹치지 않기 위해서이다.

3. 거리 설정값을 입력 가능
- 장치목록의 탐지된 장치의 오른쪽에 거리 설정값 입력 부분이 있어야 한다.
- 거리설정값 입력 부분을 클릭하면 거리 설정 값 입력 다이얼로그가 뜬다.
- 사용자가 거리 설정값을 수정하면 수정된 값이 표시되어야 한다.
- 거리 설정값의 입력 범위는 0.0~99.9 이며 표시 단위는 0.1 meter 로 한다. 
- 거리 설정값의 디폴트 값은 50.0 으로 한다
- 거리 설정값은 앱 내부에 저장되어 사용자가 변경 후 앱을 종료 했다가 재실행해도 변경된 거리 설정값을 가지고 있고 또한 표시되야 한다.

4. 비콘 부저를 울리고 끄게 할수 있음
- 탐지된 장치의 거리설정 값 표시부분의 오른쪽에 부저 알람 / 알람중지 버튼이 있어야 한다.
- 부저 알람 버튼을 수동으로 눌렀을 때 앱은 비콘과 BLE 커넥션을 하고, 부저 알람 명령을 송신한다. 
- 부저 알람 버튼의 예시는 sensordemo의 ringdevice 버튼이다.
- 부저 알람은 ringDevice()을 활용하여 수행한다. Ringtype 은 0x1 이다
- 부저 알람은 사용자가 알람중지 버튼을 누를때까지 계속 실행되어야 한다.
- 따라서 부저 알람 시작 시 스케쥴러를 이용하여 ringtime이 만료되면 만료됨을 스케쥴러로 알려주고, 스케쥴러는 링타임 만료됨을 확인하면 부저 알람을 재실행한다
- 알람중지 버튼이 수동으로 클릭되면 스케쥴러에서 부저알람 실행을 중지하고, 비콘으로도 알람 중지 명령을 송신한다. 
- 버튼이 눌릴때 동작 상태를 버튼에 표시한다.
- 부저 알람 버튼에 디폴트로 "알람" 이라는 한국어를 표시한다. 
- 부저 알람 버튼이 눌렸을때 "동작중" 을 표시하고, 비콘 연결이 완료되면 "연결됨", 부저 알람 명령을 송신하면 "알람중" 으로 표시하고, 알람중지 버튼이 눌리면 "알람"으로 돌아온다
- 알람 중지 버튼에 디폴트로 "알람 중지" 라는 한국어를 표시한다.
- 알람 중지 버튼이 눌렸을때 "동작중" 을 표시하고, 스케쥴러 중지와 비콘으로 알람 중지 명령 송신 후 " 알람 중지"로 돌아온다.
- 여러 개의 비콘이 동시에 울리고 있는 상태가 될수 있도록 한다. 따라서 스캔된 비콘들을 독립적으로 알람을 울리고 끌수 있어야 한다.
- 동작순서에 따라 후순위 동작이 완료되어 후순위 상태를 버튼에 표시하기 전에 앞선 동작의 상태가 버튼에 표시되기전 또는 표시되는 중이라면 후순위 상태를 표시한다.(덮어쓴다)

5. 태블릿 알람을 울리고 끄게 할수 있음
- 장치 목록 아래에 폰 알람 / 폰 알람중지 버튼이 있어야 한다.
- 폰 알람 버튼을 수동으로 눌렀을때 앱은 폰의 알람을 울리게 한다. 기본 알람시간은 20초이다.
- 폰 알람은 부저 알람 처럼 사용자가 알람중지 버튼을 누를때까지 계속 실행되어야 한다.
- 따라서 폰 알람 시작시 스케쥴러를 이용하여 알람시간이 만료되면 만료됨을 스케쥴러가 확인하고 폰 알람을 재실행한다.
- 폰 알람중지 버튼이 수동으로 클릭되면 스케쥴러에서 폰 알람 실행을 중지하고, 실제 폰의 알람도 중지시킨다
- 버튼이 눌릴때 동작 상태를 버튼에 표시한다.
- 폰 알람 버튼에 디폴트로 "폰 알람"이라는 한국어를 표시한다.
- 폰 알람 버튼이 눌렸을때 "동작중"으로 표시하고, 실제 폰 알람이 실행되면 "알람중"으로 표시한다.
- 폰 알람중지 버튼에 디폴트로 "폰 알람중지"라는 한국어를 표시한다.
- 폰 알람중지 버튼이 눌렸을때 "동작중"으로 표시하고, 실제 폰 알람중지가 되면 "폰 알람중지"로 돌아온다.
- 동작순서에 따라 후순위 동작이 완료되어 후순위 상태를 버튼에 표시하기 전에 앞선 동작의 상태가 버튼에 표시되기전 또는 표시되는 중이라면 후순위 상태를 표시한다.(덮어쓴다)

6. RSSI 값을 거리 값으로 변경하여 표시
- 탐지된 비콘의 RSSI 값을 이용하여 거리를 계산하여 표시한다. 1번 기능에서 작성된 장치목록의 계산된 거리값을 지칭한다.
- 이 기능은 장치 이름으로 필터링 하여 6자리로 시작하는 BLE 비콘, 즉 탐지된 비콘에 대해서만 적용한다.
- RSSI 신호는 BLE 광고모드에서만 획득될수 있고, BLE 가 연결된 상태에서는 획득될수 없다. 이것은 비콘 펌웨어 동작에 따른 결과로서 앱의 환경적 제한사항이다.
- 비콘은 nRF528XX 시리즈의 칩셋을 사용한다
- 비콘의 현재 광고주기는 200ms 이다. 
- RSSI-거리 변환 공식을 사용한다. distance(m) = 10^((txPowerAt1m - rssiFiltered)/(10 * n)) 이고 역함수는 RSSI(d) = TxPowerAt1m - 10 * n * log10(d) 이다.
- 공식의 txPowerAt1m(1미터 기준의 RSSI) 과 n을 켈리브레이션 할수 있다. 기본값은 각각 txPowerAt1m=-59, n=2.0 이다
- 탐지된 비콘의 알람중지 버튼 오른쪽에 켈리브레이션 버튼이 있다.
- 켈리브레이션 버튼에 "거리보정"이라고 한국어를 표시한다.
- 켈리브레이션 버튼을 눌렀을 때 켈리브레이션 용 API 를 제공한다.
- 켈리브레이션용 API 는 d=1m, 2m, 3m 거리에서 10초간 수행하고 다음 거리 측정 시 사용자가 위치를 이동 시킬수 있게 안내해주며 측정 시작전에 3,2,1 초 후에 시작한다고 알려준다. 
- '3회 측정후 "계산중" 이라는 한국어를 표시한다. 이 표시는 캘리브레이션 결과값이 표시되기 전까지 유지된다. 만약 계산중을 UI에 업데이트 하기 전에 결과값이 먼저 나오면 결과값을 표시한다.
- 캘리브레이션 기능은 버튼이 눌렸을때 다음 4가지 기능이 순차적으로 시행된다.
- 켈리브레이션은 첫번째 실제값 획득하기 기능이 먼저 수행되어 사용자에게 1, 2, 3 m 위치에서 각각 10초간 RSSI를 수집한 후 각 구간의 이상치 제거 후 중앙값을 r1, r2, r3로 획득한다. (중앙값 med 기준 ±7 dB 이상치 제거)
- 중앙값 획득 후  x_i = log10(d_i), y_i = r_i에 대해 선형회귀(y = a + b x) 를 수행하여 txPowerAt1m = a, n = -b / 10 로 txPowerAt1m 과 n 값을 획득하고 실제값 획득하기를 종료한다. txPowerAt1m 과 n 의 정밀도는 최대 소수점 둘째자리 까지만 사용한다.
- 켈리브레이션의 두번째 기능은 품질평가이다. 선형회귀식 y = a + b x 에서 획득한 TxPowerAt1m = a, n = -b/10 으로 residual_i = y_i - (a + b x_i) 를 계산한다.
- 그리고 다음 수식에 의해 R² 을 계산한다. R² = 1 - (SS_res / SS_tot), 여기서 SS_res = Σ (y_i - ŷ_i)^2, SS_tot = Σ (y_i - ȳ)^2, 여기서 y_i : 실제 RSSI 중앙값, ŷ_i : 회귀식으로 예측한 RSSI, ȳ : 실제 RSSI들의 평균
- 그리고 다음의 규칙에 따라 각 측정 값이 이상치인지 판별한다. 규칙 A(강건): |z_i| > 3 이면 포인트 i를 이상치로 판단. 규칙 B(절대 dB 기준): |residual_i| > 5 dB 도 이상치로 판단. 품질평가의 결과는 R² 과 각 이상치A,B의 개수이다
- 켈리브레이션의 세번째 기능은 품질판정이다. GOOD(합격): R² ≥ 0.90 그리고 RMSE ≤ 2.0 dB 그리고 max|residual_i| ≤ 4 dB , BORDERLINE(주의): 0.80 ≤ R² < 0.90 또는 2.0 < RMSE ≤ 3.0 dB, BAD(재측정): R² < 0.80 또는 RMSE > 3.0 dB 또는 이상치 ≥ 2개
- 품질판정 기능의 결과는 GOOD, BORDERLIN, BAD 중에 하나이다.
- 켈리브레이션의 네번째 이자 마지막 기능은 저장이다. 품질판정 단계에서 Good과 BORDERLINE인 경우 TxPowerAt1m, n, R², RMSE 을 앱에 저장한다. 앱 내부에 저장되어 사용자가 앱을 종료 했다가 재실행해도 값을 가지고 있고 또한 사용되야 한다.
- Good인 경우 이어서 사용자에게  TxPowerAt1m, n, R², RMSE 값(캘레브레이션 결과값)을 알려주고 켈리브레이션용 API를 종료한다.
- BORDERLINE인 경우 이어서 사용자에게  TxPowerAt1m, n, R², RMSE 값(캘레브레이션 결과값)을 알려주고 "보정 결과 BORDERLINE. 재측정을 원하면 시행하세요"고 알려주고 켈리브레이션용 API를 종료한다.
- BAD인 경우는 사용자에게 TxPowerAt1m, n, R², RMSE 값(캘레브레이션 결과값)을 알려주고 "보정 결과 BAD. 재측정을 시행하세요"고 알려주고 켈리브레이션용 API를 종료한다.
- RSSI 값 취득하여 거리를 계산하려면, 첫번째로 RSSI 전처리를 탐지된 비콘 별로 각각 시행한다.
- 전처리 기능은 최근 N=10 샘플링 윈도우를 유지하고(무빙 윈도우) 중앙값 med 기준  ±7 dB 이상치 제거하고 지수이동평균(EMA)를 사용한다. 
- 지수 이동평균식은 다음과 같다. EMA_new = α * X_new + (1 - α) * EMA_prev, 여기서 X_new: 새로 들어온 값 (RSSI), EMA_prev: 직전까지의 EMA 값, α=0.25, EMA_new 값을 rssiFiltered 에 할당하고 RSSI 전처리를 마친다.
- 두번째로 거리 계산 기능은 distance(m) = 10^((txPowerAt1m - rssiFiltered)/(10 * n) 을 이용하여 거리를 계산한다. 이때 txPowerAt1m와 n은 켈리브레이션 결과를 사용하고, 결과값이 없으면 디폴트를 사용한다.
- Distance는 지수이동평균(α=0.30)을 거치고 최종 거리값 distanceFiltered에 할당한다. 거리계산 기능을 마친다. 지수이동평균식은 RSSI 와 distance에 사용될때 α 값이 다르므로 혼용되지 않도록 구조를 주의한다.

E. 앱 알고리즘 구현시 주의사항
- 앱의 주요 동작은 distanceFiltered 값이 사용자가 설정한 거리값을 초과하면 탐지된 비콘의 부저를 울리게 하고 동시에 폰 알람을 울리게 한다.
- 켈리브레이션 중에는 사용자 거리 값을 초과해도 부저와 폰을 울리지 않는다.
- 따라서 앱의 알고리즘은 앱 실행과 동시에 항상 스캔을 시작하며 탐지된 비콘의 거리 설정값을 초과하는 distanceFiltered 가 탐지되면 BLE 비콘을 커넥트를 하고 부저 알람 명령을 송신하고 디스커넥트 하고 폰 알람을 실행한다
- 이 알고리즘을 자동기능이라 지칭한다. 자동기능은 수동버튼의 로직을 재사용하여 수동 버튼을 스케쥴링하는 모드이다. 즉 기본기능은 언제나 개별적으로 수행되고 자동기능은 "설정값 초과하는 조건시 알람 실행"만 수행하는 것이다.
- 따라서 자동기능중에 모든 수동기능은 언제나 입력 및 동작 가능해야 한다.
- 예를들어 앱 실행과 동시에 항상 스캔을 실행하는 것은 사용자가 스캔 버튼을 눌러서 시작하는 동작의 초기값이 앱 실행시 버튼 눌렸음으로 할당되었고 이로 인해 스캔이 시작된것이다.
- 또한 탐지된 비콘은 여러 개가 동시에 커넥트 되고 울릴수 있다, 그러나 폰 알람은 1개이다.
- 따라서 자동 기능 실행 시 탐지된 비콘 여러개를 울리게 될 경우 폰 알람 로직은 재사용되어 폰 알람을 울리게 된다. 비콘당 별도의 폰 알람이 존재하는것이 아니다.

F. 실행환경
- 앱의 모든 기능은 백그라운드에서도 실행되고 유지 되어야 한다. 
- 따라서 사용자가 홈버튼으로 폰의 홈화면으로 갔다가 앱을 오버뷰화면에서 다시 선택하거나, 홈버튼으로 폰의 홈화면으로 갔다가 앱아이콘을 눌러 앱을 실행해도 탐지된 비콘 목록과 거리 설정값, 켈리브레이션값은 유지되어야 한다.
- 앱을 폰의 오버뷰화면에서 완전히 종료시켜도 탐지되었던 비콘의 배터리 수준, 거리 설정값, 켈리브레이션 값은 해당 탐지된 비콘의 이름에 대해 저장되어 있어야 한다.
- 따라서 다음에 새로운 비콘이 아닌 탐지되었던 비콘이 재탐지되는 경우 같은 장치 이름에 대해 배터리 수준, 거리 설정값, 겔리브레이션 값은 저장되었다가 초기값으로 사용되어야 한다.
- 탐지된 비콘에 대한 정보를 저장하는 것은 최소 20개 이상의 비콘 정보를 저장할수 있다.

G. UI 구성
- 앱 사용자는 저시력자의 가능성이 매우 높으므로 앱 내부에 표시되는 모든 문자와 숫자의 폰트는 저시력자를 위해 가능한 크게 한다. 이것은 버튼 배치와 화면 배치등 UI 에서 표현가능한 수준내에서 크게 한다.
- 버튼, 목록, 사용자 입력 등의 화면 표시 및 입력부는 사용자 클릭시 오브젝트가 중복되어 선택되지 않도록 겹치지 않게 구성한다.

H. 기타
- 사용자 요청에 따른 코드 수정 및 생성등 변화가 있는 경우, 변화 내용에 대해 깃허브에 커밋할수 있도록 요약해서 커밋 메시지를 생성하여 출력한다.
- 'Conventional Commits' 형식의 커밋 메시지를 아래와 같은 가이드에 맟추어 생성한다.
- 한국어 제목(50자 이내) + 필요한 경우 본문(한 줄당 72자 이내, 불릿 OK)
- 변경 의도와 범위를 간결히 요약
- BREAKING CHANGE나 이슈 번호가 있으면 footer에 추가


I. UI 구성 추가
아래와 같은 구성으로 UI 레이아웃을 구성한다. + 로 화면의 구획을 나타내었다.

++++++++++++++++++++++++++++++++++++++++++++++++++++++++
비콘이름                +   부저알람버튼   +  부저알람중지버튼
RSSI                 +             +
배터리수준              + +++++++++++++++++++++++++++++++++++
계산된 거리            +   거리설정버튼     +  캘리브레이션버튼
++++++++++++++++++++++++++++++++++++++++++++++++++++++++
두번째 비콘            +
                        +
                        +
                        +
++++++++++++++++++++++++++++++++++++++++++++++++++++++++

++++++++++++++++++++++++++++++++++++++++++++++++++++++++
폰 알람 버튼                       +       폰 알람중지 버튼
++++++++++++++++++++++++++++++++++++++++++++++++++++++++

J. 기타사항 추가
이 앱은 단일화면으로 구성된다. 뷰페이지는 1개이다. 데모프로젝트의 필요없는 부분은 삭제한다.

```

